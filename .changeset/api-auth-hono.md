---
'@rtorcato/api-auth-hono': minor
---

Add `@rtorcato/api-auth-hono` — Hono middleware over `@rtorcato/api-auth`. Provides `authMiddleware` and `optionalAuthMiddleware` with Bearer-header and cookie token support, and an `AuthVariables` type for a typed `c.get('user')`.
