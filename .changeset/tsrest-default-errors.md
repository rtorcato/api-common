---
"@rtorcato/api-ts-rest-express": minor
---

Add `withDefaultErrors` and `defaultErrorSchema` for attaching the shared `400`/`404`/`500` error envelope (matching `@rtorcato/api-errors-express`) to a ts-rest route's `responses`, plus `RestRequest` / `RestResponse` inference-type aliases for typing handlers off a contract.
